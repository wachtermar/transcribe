[project]
name = "transcribe"
version = "0.1.0"
description = "Audio transcription with speaker detection using Gemini"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.65.0",
    "rich>=14.0.0",
]

[project.scripts]
transcribe = "transcribe:main"
